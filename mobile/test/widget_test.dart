import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Greenova app basic test', (WidgetTester tester) async {
    // Basic placeholder test - app requires Hive initialization
    expect(1 + 1, equals(2));
  });
}
